@model SQMS.Models.tblDailyBreak

<div class="container" style="width:600px">
    <div class="row">

        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()
            <div class="form-horizontal">

                @Html.ValidationSummary(true, "", new { @class = "text-danger" })

                <div class="form-group">
                    <label class="control-label col-md-2">User </label>
                    @* @Html.LabelFor(model => model.user_id, null, htmlAttributes: new { @class = "control-label col-md-2" }) *@
                    <div class="col-md-6">
                        <select class="form-control" name="user_id" id="user_id" disabled="disabled">
                            @foreach (var item in ViewBag.user_id)
                            {
                                <option value="@item.Id">@item.Hometown</option>
                            }
                        </select>
                        @* @Html.DropDownList("user_id", null, htmlAttributes: new { @class = "form-control", disabled = "disabled" }) *@
                        @Html.ValidationMessageFor(model => model.user_id, "", new { @class = "text-danger" })
                    </div>
                </div>

                <div class="form-group">
                    <label class="control-label col-md-2">Break Type</label>
                    @* @Html.LabelFor(model => model.break_type_id, null, htmlAttributes: new { @class = "control-label col-md-2" }) *@
                    <div class="col-md-6">
                        <select class="form-control" name="break_type_id" id="break_type_id" required>
                            @foreach (var item in ViewBag.break_type_id)
                            {
                                <option value="@item.break_type_id">@item.break_type_with_duration</option>
                            }
                        </select>
                        @* @Html.DropDownList("break_type_id", null, htmlAttributes: new { @class = "form-control" }) *@
                        @Html.ValidationMessageFor(model => model.break_type_id, "", new { @class = "text-danger" })
                    </div>
                </div>

                <div class="form-group">
                    <label class="control-label col-md-2">Remarks</label>
                    @* @Html.LabelFor(model => model.remarks, htmlAttributes: new { @class = "control-label col-md-2" }) *@
                    <div class="col-md-6">
                        @Html.TextAreaFor(model => model.remarks, new { @class = "form-control Next", @row = "5" })
                        @Html.ValidationMessageFor(model => model.remarks, "", new { @class = "text-danger" })
                    </div>
                </div>


            </div>
        }

    </div>

</div>









@* 
<div class="container" style="width:600px">
    <div class="row">

        <form asp-action="Create" asp-controller="DailyBreaks" method="post">
            @Html.AntiForgeryToken()

            <div class="form-horizontal">

                <partial name="_ValidationSummary" />

                <div class="form-group">
                    <label asp-for="user_id" class="control-label col-md-2"></label>
                    <div class="col-md-6">
                        <select class="form-control" name="user_id" id="user_id" required>
                            <option value="">Please select</option>
                            @foreach (var item in ViewBag.branch_id)
                            {
                                <option value="@item.Id">@item.Hometown</option>
                            }
                        </select>
                        <span asp-validation-for="user_id" class="text-danger"></span>
                    </div>
                </div>

                <div class="form-group">
                    <label asp-for="break_type_id" class="control-label col-md-2"></label>
                    <div class="col-md-6">
                        <select class="form-control" name="break_type_id" id="break_type_id" required>
                            <option value="">Please select</option>
                            @foreach (var item in ViewBag.branch_id)
                            {
                                <option value="@item.break_type_id">@item.break_name_with_duration</option>
                            }
                        </select>
                        <span asp-validation-for="break_type_id" class="text-danger"></span>
                    </div>
                </div>

                <div class="form-group">
                    <label asp-for="remarks" class="control-label col-md-2"></label>
                    <div class="col-md-6">
                        <textarea asp-for="remarks" class="form-control Next" rows="5"></textarea>
                        <span asp-validation-for="remarks" class="text-danger"></span>
                    </div>
                </div>

            </div>

            <button type="submit" class="btn btn-default">Save</button>
        </form>


    </div>

</div>
    *@